export interface InconeType {
  title: string;
  query: string;
}

export interface InconeChartType {
  month: string;
  Income: number;
  Expense: number;
}

export interface ExpenseChartType {
  category: string;
  visitors: number;
  fill: string;
}
